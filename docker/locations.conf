# Additional location configurations for the app
# This file is included by nginx.conf

# Add any app-specific location blocks here if needed

# Serve static files directly
location /rttlinfo/static/ {
    alias /static/;
    expires 1y;
    add_header Cache-Control "public, immutable";
    access_log off;
}